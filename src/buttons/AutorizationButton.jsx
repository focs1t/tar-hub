import React from 'react';

const AutorizationButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>Открыть форму авторизации</button>
  );
};

export default AutorizationButton;
