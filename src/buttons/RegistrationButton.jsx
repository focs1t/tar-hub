import React from 'react';

const RegistrationButton = ({ onClick }) => {
  return (
    <button onClick={onClick}>Открыть форму регистрации</button>
  );
};

export default RegistrationButton;
